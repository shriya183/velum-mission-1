import React from "react";

const Header: React.FC = () => {
  return (
    <header className="absolute top-0 left-0 right-0 z-50 py-4 px-6 flex justify-between items-center">
      {/* Header container kept empty */}
    </header>
  );
};

export default Header;
